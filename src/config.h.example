#ifndef CONFIG_H
#define CONFIG_H

// WiFi
#define CFG_WIFI_ESSID "xxxxxxxx"
#define CFG_WIFI_PASSWORD "xxxxxxxx"

// mDNS
#define CFG_HOSTNAME "esp8266"

// InfluxDB
#define CFG_INFLUXDB_URL "influxdb-url"  // InfluxDB server url, e.g. http://192.168.1.48:8086 (Use: InfluxDB UI -> Load Data -> Client Libraries)
//#define CFG_INFLUXDB_1_DB_NAME "bucket"  // InfluxDB 1 database name. If defined, InfluxDB 1 API is used instead of 2, and TOKEN/ORG/BUCKET are ignored.
#define CFG_INFLUXDB_TOKEN "token"  // InfluxDB 2 server or cloud API authentication token (Use: InfluxDB UI -> Load Data -> Tokens -> <select token>)
#define CFG_INFLUXDB_ORG "org"  // InfluxDB 2 organization name or id (Use: InfluxDB UI -> Settings -> Profile -> <name under tile> )
#define CFG_INFLUXDB_BUCKET "bucket"  // InfluxDB 2 bucket name (Use: InfluxDB UI -> Load Data -> Buckets)

#define CFG_MEASUREMENT_NAME "esp8266_influx_demo"
#define CFG_DEVICE_NAME_TAG "esp8266"  // Defaults to CFG_HOSTNAME if not defined

#endif //CONFIG_H
